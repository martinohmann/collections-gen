// +build !ignore_autogenerated

// Code generated by collections-gen. DO NOT EDIT.

package underlying

import (
	"io"
	"reflect"
	"sort"
)

// ReaderMapUnderlyingCollection is a collection of map[string]io.Reader values.
type ReaderMapUnderlyingCollection struct {
	items []map[string]io.Reader
}

// NewReaderMapUnderlyingCollection creates a new collection from a slice of map[string]io.Reader.
func NewReaderMapUnderlyingCollection(items []map[string]io.Reader) *ReaderMapUnderlyingCollection {
	return &ReaderMapUnderlyingCollection{items}
}

// Interface returns the underlying slice used by the collection as interface{}
// value.
func (c *ReaderMapUnderlyingCollection) Interface() interface{} {
	return c.items
}

// Items returns the underlying slice of map[string]io.Reader values used by the
// collection.
func (c *ReaderMapUnderlyingCollection) Items() []map[string]io.Reader {
	return c.items
}

// EachIndex calls fn for every item in the collection. The slice index of the
// item is passed to fn as the second argument.
func (c *ReaderMapUnderlyingCollection) EachIndex(fn func(map[string]io.Reader, int)) {
	for i, item := range c.items {
		fn(item, i)
	}
}

// Each calls fn for every item in the collection.
func (c *ReaderMapUnderlyingCollection) Each(fn func(map[string]io.Reader)) {
	c.EachIndex(func(item map[string]io.Reader, _ int) {
		fn(item)
	})
}

// IndexOf searches for el in the collection and returns the first index where
// el is found. If el is not present in the collection IndexOf will return -1.
func (c *ReaderMapUnderlyingCollection) IndexOf(el map[string]io.Reader) int {
	for i, item := range c.items {
		if reflect.DeepEqual(item, el) {
			return i
		}
	}

	return -1
}

// First returns the first item from the collection. Will panic if the
// underlying slice is empty.
func (c *ReaderMapUnderlyingCollection) First() map[string]io.Reader {
	return c.Nth(0)
}

// FirstN returns the first n map[string]io.Reader items of the collection. Will
// return less than n items if the underlying slice's length is < n.
func (c *ReaderMapUnderlyingCollection) FirstN(n int) []map[string]io.Reader {
	if n > c.Len() {
		return c.Items()
	}

	return c.Slice(0, n)
}

// Last returns the last item from the collection. Will panic if the underlying
// slice is empty.
func (c *ReaderMapUnderlyingCollection) Last() map[string]io.Reader {
	return c.Nth(c.Len() - 1)
}

// LastN returns the last n map[string]io.Reader items of the collection. Will return
// less than n items if the underlying slice's length is < n.
func (c *ReaderMapUnderlyingCollection) LastN(n int) []map[string]io.Reader {
	if c.Len()-n < 0 {
		return c.Items()
	}

	return c.Slice(c.Len()-n, c.Len())
}

// Get returns the item at pos from the collection. Will panic if the
// underlying slice is shorter than pos+1.
func (c *ReaderMapUnderlyingCollection) Get(pos int) map[string]io.Reader {
	return c.Nth(pos)
}

// Nth returns the nth item from the collection. Will panic if the underlying
// slice is shorter than pos+1.
func (c *ReaderMapUnderlyingCollection) Nth(pos int) map[string]io.Reader {
	return c.items[pos]
}

// Len returns the length of the underlying map[string]io.Reader slice.
func (c *ReaderMapUnderlyingCollection) Len() int {
	return len(c.items)
}

// Cap returns the capacity of the underlying map[string]io.Reader slice.
func (c *ReaderMapUnderlyingCollection) Cap() int {
	return cap(c.items)
}

// Append appends items and returns the collection.
func (c *ReaderMapUnderlyingCollection) Append(items ...map[string]io.Reader) *ReaderMapUnderlyingCollection {
	c.items = append(c.items, items...)
	return c
}

// Prepend prepends items and returns the collection.
func (c *ReaderMapUnderlyingCollection) Prepend(items ...map[string]io.Reader) *ReaderMapUnderlyingCollection {
	c.items = append(items, c.items...)
	return c
}

// Copy creates a copy of the collection and the underlying map[string]io.Reader slice.
func (c *ReaderMapUnderlyingCollection) Copy() *ReaderMapUnderlyingCollection {
	s := make([]map[string]io.Reader, c.Len(), c.Len())
	copy(s, c.items)

	return NewReaderMapUnderlyingCollection(s)
}

// Filter removes all items from the collection for which fn evaluates to
// false and returns c.
func (c *ReaderMapUnderlyingCollection) Filter(fn func(map[string]io.Reader) bool) *ReaderMapUnderlyingCollection {
	s := c.items[:0]

	for _, item := range c.items {
		if fn(item) {
			s = append(s, item)
		}
	}

	var zeroValue map[string]io.Reader

	for i := len(s); i < len(c.items); i++ {
		c.items[i] = zeroValue
	}

	c.items = s

	return c
}

// Collect removes all items from the collection for which fn evaluates to
// false and returns c.
func (c *ReaderMapUnderlyingCollection) Collect(fn func(map[string]io.Reader) bool) *ReaderMapUnderlyingCollection {
	return c.Filter(fn)
}

// Reject removes all items from the collection for which fn evaluates to
// true and returns c.
func (c *ReaderMapUnderlyingCollection) Reject(fn func(map[string]io.Reader) bool) *ReaderMapUnderlyingCollection {
	return c.Filter(func(v map[string]io.Reader) bool {
		return !fn(v)
	})
}

// Partition partitions the collection into two new collections. The first
// collection contains all items where fn evaluates to true, the second one all
// items where fn evaluates to false.
func (c *ReaderMapUnderlyingCollection) Partition(fn func(map[string]io.Reader) bool) (*ReaderMapUnderlyingCollection, *ReaderMapUnderlyingCollection) {
	lhs := make([]map[string]io.Reader, 0, c.Len())
	rhs := make([]map[string]io.Reader, 0, c.Len())

	for _, item := range c.items {
		if fn(item) {
			lhs = append(lhs, item)
		} else {
			rhs = append(rhs, item)
		}
	}

	return NewReaderMapUnderlyingCollection(lhs), NewReaderMapUnderlyingCollection(rhs)
}

// Map calls fn for each item in the collection an replaces its value with the
// result of fn.
func (c *ReaderMapUnderlyingCollection) Map(fn func(map[string]io.Reader) map[string]io.Reader) *ReaderMapUnderlyingCollection {
	return c.MapIndex(func(item map[string]io.Reader, _ int) map[string]io.Reader {
		return fn(item)
	})
}

// MapIndex calls fn for each item in the collection an replaces its value with the
// result of fn.
func (c *ReaderMapUnderlyingCollection) MapIndex(fn func(map[string]io.Reader, int) map[string]io.Reader) *ReaderMapUnderlyingCollection {
	for i, item := range c.items {
		c.items[i] = fn(item, i)

	}

	return c
}

// Reduce calls fn for each item in c and reduces the result into reducer. The
// reducer contains the value returned by the call to fn for the previous item.
// Reducer will be the zero map[string]io.Reader value on the first invocation.
func (c *ReaderMapUnderlyingCollection) Reduce(fn func(reducer map[string]io.Reader, item map[string]io.Reader) map[string]io.Reader) map[string]io.Reader {
	var reducer map[string]io.Reader

	for _, item := range c.items {
		reducer = fn(reducer, item)
	}

	return reducer
}

// Find returns the first item for which fn evaluates to true. If the
// collection does not contain a matching item, Find will return the zero
// map[string]io.Reader value. If you need to distinguish zero values from a condition
// that did not match any item consider using FindOk instead.
func (c *ReaderMapUnderlyingCollection) Find(fn func(map[string]io.Reader) bool) map[string]io.Reader {
	item, _ := c.FindOk(fn)

	return item
}

// FindOk returns the first item for which fn evaluates to true. If the
// collection does not contain a matching item, FindOk will return the zero
// map[string]io.Reader value. The second return value denotes whether the condition
// matched any item or not.
func (c *ReaderMapUnderlyingCollection) FindOk(fn func(map[string]io.Reader) bool) (map[string]io.Reader, bool) {
	for _, item := range c.items {
		if fn(item) {
			return item, true
		}
	}

	var zeroValue map[string]io.Reader
	return zeroValue, false
}

// Any returns true as soon as fn evaluates to true for one item in c.
func (c *ReaderMapUnderlyingCollection) Any(fn func(map[string]io.Reader) bool) bool {
	for _, item := range c.items {
		if fn(item) {
			return true
		}
	}

	return false
}

// All returns true if fn evaluates to true for all items in c.
func (c *ReaderMapUnderlyingCollection) All(fn func(map[string]io.Reader) bool) bool {
	for _, item := range c.items {
		if !fn(item) {
			return false
		}
	}

	return true
}

// Contains returns true if the collection contains el.
func (c *ReaderMapUnderlyingCollection) Contains(el map[string]io.Reader) bool {
	for _, item := range c.items {
		if reflect.DeepEqual(item, el) {
			return true
		}
	}

	return false
}

// Sort sorts the collection using the passed in comparator func.
func (c *ReaderMapUnderlyingCollection) Sort(fn func(map[string]io.Reader, map[string]io.Reader) bool) *ReaderMapUnderlyingCollection {
	sort.Slice(c.items, c.lessFunc(fn))
	return c
}

// IsSorted returns true if the collection is sorted in the order defined by
// the passed in comparator func.
func (c *ReaderMapUnderlyingCollection) IsSorted(fn func(map[string]io.Reader, map[string]io.Reader) bool) bool {
	return sort.SliceIsSorted(c.items, c.lessFunc(fn))
}

func (c *ReaderMapUnderlyingCollection) lessFunc(fn func(map[string]io.Reader, map[string]io.Reader) bool) func(int, int) bool {
	return func(i, j int) bool {
		return fn(c.items[i], c.items[j])
	}
}

// Reverse reverses the order of the collection items in place and returns c.
func (c *ReaderMapUnderlyingCollection) Reverse() *ReaderMapUnderlyingCollection {
	for l, r := 0, len(c.items)-1; l < r; l, r = l+1, r-1 {
		c.items[l], c.items[r] = c.items[r], c.items[l]
	}

	return c
}

// Remove removes the collection item at position pos. Will panic if pos is out
// of bounds.
func (c *ReaderMapUnderlyingCollection) Remove(pos int) *ReaderMapUnderlyingCollection {
	c.items = append(c.items[:pos], c.items[pos+1:]...)
	return c
}

// RemoveItem removes all instances of item from the collection and returns it.
func (c *ReaderMapUnderlyingCollection) RemoveItem(item map[string]io.Reader) *ReaderMapUnderlyingCollection {
	for i, el := range c.items {
		if reflect.DeepEqual(el, item) {
			c.items = append(c.items[:i], c.items[i+1:]...)
		}
	}

	return c
}

// InsertItem inserts item into the collection at position pos. Will panic if
// pos is out of bounds.
func (c *ReaderMapUnderlyingCollection) InsertItem(item map[string]io.Reader, pos int) *ReaderMapUnderlyingCollection {
	var zeroValue map[string]io.Reader
	c.items = append(c.items, zeroValue)
	copy(c.items[pos+1:], c.items[pos:])
	c.items[pos] = item
	return c
}

// Cut returns a copy of the underlying map[string]io.Reader slice with the items
// between index i and j removed. Will panic if i or j is out of bounds of the
// underlying slice.
func (c *ReaderMapUnderlyingCollection) Cut(i, j int) []map[string]io.Reader {
	s := make([]map[string]io.Reader, 0, c.Len())
	s = append(s, c.items[:i]...)
	return append(s, c.items[j:]...)
}

// Slice returns the map[string]io.Reader items between slice index i and j. Will
// panic if i or j is out of bounds.
func (c *ReaderMapUnderlyingCollection) Slice(i, j int) []map[string]io.Reader {
	return c.items[i:j]
}
